<%= form_for(@user_tweet) do |f| %>
  <% if @user_tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_tweet.errors.count, "error") %> prohibited this user_tweet from being saved:</h2>

      <ul>
      <% @user_tweet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    おそらくここはsessionを入れておいて隠しておく必要がある。
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :tweet %><br />
    <%= f.text_area :tweet %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
